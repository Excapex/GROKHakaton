# Python zavisnosti za A (validacija ugovora, kasnije ingest).
# Instalacija: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
jsonschema>=4.23.0
pymupdf>=1.24.0
python-docx>=1.1.0
openpyxl>=3.1.0
